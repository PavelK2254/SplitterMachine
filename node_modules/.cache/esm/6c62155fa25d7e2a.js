let MongoClient;_a25‍.x([["init",()=>init],["addNewOrdersToDB",()=>addNewOrdersToDB],["getOrderByBarcode",()=>getOrderByBarcode]]);_a25‍.w("mongodb",[["MongoClient",["MongoClient"],function(v){MongoClient=v}]]);const collectionName = "ordersTest";
const options = { useNewUrlParser: false, useUnifiedTopology: false };
const dbName = 'splitterDB'

var url = "mongodb://localhost:27017/" + dbName;
var availableCollections = [];

       function init() {
    return new Promise(function(resolve, reject) {
        MongoClient.connect(url, options, function(err, db) {
            if (err) {
                reject(err)
            };
            var dbo = db.db(dbName);
            dbo.listCollections().toArray(function(err, items) {
                if (err) {
                    reject(err)
                };
                availableCollections = items;
                if (items.length == 0) {
                    console.log(`No collections in database, creating ${collectionName}`)
                    dbo.createCollection(collectionName, function(err, res) {
                        if (err) {
                            reject(err)
                        };
                        resolve(`Collection created! ${collectionName}`);
                        db.close();
                    });
                } else {
                    resolve(`Collection ${collectionName} Is available`);
                }
            });
        });
    })
}

       function addNewOrdersToDB(order) {
    return new Promise(function(resolve, reject) {
        MongoClient.connect(url, options, function(err, db) {
            if (err) throw err;
            var dbo = db.db(dbName);
            var newOrder = order;
            dbo.collection(collectionName).insertMany(newOrder, function(err, res) {
                if (err) {
                    db.close();
                    reject(err);
                    _a25‍.g.console.log(err);
                } else {
                    resolve(res);
                    db.close();
                }
            });
        });
    })

}



       function getOrderByBarcode(barcode) {
    return new Promise(function(resolve, reject) {
        MongoClient.connect(url, function(err, db) {
            if (err) reject(err);
            var dbo = db.db(dbName);
            dbo.collection(collectionName).findOne({ barcodeNum: +barcode }, function(err, result) {
                if (err) {
                    reject(err);
                } else {
                    if (result != null) {
                        resolve(`Lane: ${result.orderNum}`);
                    } else {
                        reject(`No item found for barcode ${barcode}`);
                    }
                }
                db.close();
            });
        });
    });
}