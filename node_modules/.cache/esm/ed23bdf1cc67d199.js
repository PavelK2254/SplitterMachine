let PlcResultModel,getSingleOrder;_cc6‍.x([["parseOrderData",()=>parseOrderData]]);_cc6‍.w("../Models/plcResultModel",[["PlcResultModel",["PlcResultModel"],function(v){PlcResultModel=v}]]);_cc6‍.w("../Controller/ordersFetcher",[["getSingleOrder",["getSingleOrder"],function(v){getSingleOrder=v}]]);


const STATIC_LANE = "2"

       async function parseOrderData(socketString) {
    return new Promise(function(resolve, reject) {
        var barcodeObject;
        try {
            if (isJson(socketString)) {
                barcodeObject = JSON.parse(socketString)
                barcodeObject.forEach(element => {
                    getSingleOrder(element.tracingNum).then((result) => {
                        var modResult = result;
                        resolve(new PlcResultModel("true", modResult.toString()))
                    }).catch((err) => {
                        reject(new PlcResultModel("false", "Error: " + err));
                    })
                });
            } else {
                reject(new PlcResultModel("false", "Invalid JSON Object: " + socketString));
            }

        } catch (error) {
            _cc6‍.g.console.log(error);
            reject(error);
        }
    })
}

function isJson(str) {
    try {
        JSON.parse(str);
    } catch (e) {
        return false;
    }
    return true;
}