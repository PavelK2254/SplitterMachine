let PlcResultModel,getSingleOrder;_038‍.x([["parseOrderData",()=>parseOrderData]]);_038‍.w("../Models/plcResultModel",[["PlcResultModel",["PlcResultModel"],function(v){PlcResultModel=v}]]);_038‍.w("../Controller/ordersFetcher",[["getSingleOrder",["getSingleOrder"],function(v){getSingleOrder=v}]]);


const STATIC_LANE = "2"

       async function parseOrderData(socketString) {
    return new Promise(function(resolve, reject) {
        var barcodeObject;
        try {
            if (isJson(socketString)) {
                barcodeObject = JSON.parse(socketString)
                barcodeObject.forEach(element => {
                    getSingleOrder(element.tracingNum).then((result) => {
                        var modResult = result;
                        resolve(new PlcResultModel("true", modResult.toString()))
                    }).catch((err) => {
                        reject(new PlcResultModel("false", "Error: " + err));
                    })
                });
            } else {
                reject(new PlcResultModel("false", "Invalid JSON Object: " + socketString));
            }

        } catch (error) {
            _038‍.g.console.log(error);
            reject(error);
        }
    })
}

function isJson(str) {
    try {
        JSON.parse(str);
    } catch (e) {
        return false;
    }
    return true;
}